# 云端部署指南

## 项目概述
您的项目是一个 Ionic Vue + Capacitor 混合应用，包含：
- **前端**: Ionic Vue 应用（可部署为 Web 应用）
- **后端**: Node.js + Express + MySQL 服务器
- **移动端**: Android 应用（通过 Capacitor 构建）

## 部署方案

### 1. 前端 Web 应用部署

#### 方案 A: Vercel（推荐）
```bash
# 1. 安装 Vercel CLI
npm i -g vercel

# 2. 在项目根目录登录
vercel login

# 3. 构建项目
npm run build

# 4. 部署
vercel --prod
```

#### 方案 B: Netlify
```bash
# 1. 安装 Netlify CLI
npm i -g netlify-cli

# 2. 构建项目
npm run build

# 3. 部署
netlify deploy --prod --dir=dist
```

#### 方案 C: GitHub Pages
```bash
# 1. 构建项目
npm run build

# 2. 将 dist 文件夹推送到 gh-pages 分支
# 在 GitHub 仓库设置中启用 Pages
```

### 2. 后端 API 部署

#### 方案 A: Railway（推荐）
```bash
# 1. 在 backend 目录创建 railway.json
{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run dev",
    "healthcheckPath": "/"
  }
}

# 2. 连接 Railway
railway login
railway init
railway up
```

#### 方案 B: Heroku
```bash
# 1. 在 backend 目录创建 Procfile
web: npm run dev

# 2. 安装 Heroku CLI 并部署
heroku create your-app-name
git push heroku main
```

#### 方案 C: DigitalOcean App Platform
- 通过 Web 界面创建应用
- 连接 GitHub 仓库
- 选择 Node.js 环境
- 设置环境变量

### 3. 数据库部署

#### 方案 A: PlanetScale（MySQL 云服务）
1. 注册 PlanetScale 账户
2. 创建数据库
3. 获取连接字符串
4. 更新后端环境变量

#### 方案 B: Railway MySQL
```bash
# 在 Railway 项目中添加 MySQL 服务
railway add mysql
```

#### 方案 C: Supabase（PostgreSQL）
1. 注册 Supabase 账户
2. 创建项目
3. 获取数据库连接信息
4. 修改后端代码适配 PostgreSQL

### 4. 移动端应用部署

#### Android APK 构建和分发
```bash
# 1. 构建 Web 应用
npm run build

# 2. 同步到 Android
npx cap sync android

# 3. 打开 Android Studio
npx cap open android

# 4. 在 Android Studio 中构建 APK
# Build -> Build Bundle(s) / APK(s) -> Build APK(s)
```

#### 分发平台
- **Google Play Store**: 官方应用商店
- **APKPure**: 第三方应用商店
- **直接分发**: 通过文件分享或企业分发

## 环境变量配置

### 前端环境变量
创建 `.env.production` 文件：
```env
VITE_API_BASE_URL=https://your-backend-url.com
VITE_APP_NAME=TestApp
```

### 后端环境变量
创建 `.env` 文件：
```env
PORT=3000
DB_HOST=your-database-host
DB_USER=your-database-user
DB_PASSWORD=your-database-password
DB_NAME=your-database-name
NODE_ENV=production
```

## 部署步骤

### 第一步：准备代码
```bash
# 1. 确保代码已提交到 Git
git add .
git commit -m "准备部署"
git push origin main

# 2. 检查是否有敏感信息需要移除
# 确保 .env 文件在 .gitignore 中
```

### 第二步：部署后端
```bash
cd backend
# 选择上述任一后端部署方案
# 配置数据库连接
# 设置环境变量
```

### 第三步：部署前端
```bash
# 更新 API 地址
# 构建并部署前端
npm run build
# 选择上述任一前端部署方案
```

### 第四步：构建移动端
```bash
# 更新 API 地址
# 构建移动端应用
npx cap sync android
npx cap open android
```

## 监控和维护

### 日志监控
- 使用云服务提供商的日志服务
- 设置错误告警
- 定期检查应用状态

### 性能优化
- 启用 CDN
- 压缩静态资源
- 数据库查询优化

### 安全考虑
- 使用 HTTPS
- 设置 CORS 策略
- 定期更新依赖
- 数据库访问控制

## 成本估算

### 免费方案
- **Vercel**: 免费额度足够小型项目
- **Railway**: 每月 $5 免费额度
- **PlanetScale**: 免费数据库额度

### 付费方案
- **Vercel Pro**: $20/月
- **Railway**: $5/月起
- **PlanetScale**: $29/月起

## 故障排除

### 常见问题
1. **CORS 错误**: 检查后端 CORS 配置
2. **API 连接失败**: 检查环境变量和网络配置
3. **构建失败**: 检查依赖版本和构建脚本
4. **数据库连接失败**: 检查连接字符串和权限

### 调试工具
- 浏览器开发者工具
- 云服务商的控制台
- 日志分析工具

## 下一步
1. 选择适合的部署方案
2. 按照步骤进行部署
3. 测试所有功能
4. 设置监控和告警
5. 准备用户文档
